# photo-metadata-analyzer
사진 메타데이터 분석기 (Python + CustomTkinter)

파이썬으로 메타데이터 분석하는 프로그램

**필요 패키지**
- `customtkinter`
- `Pillow`
- `piexif` 또는 `exifread`

패키지 설치

```bash
python -m pip install -r requirements.txt
```

**실행**

```bash
python main.py
```
